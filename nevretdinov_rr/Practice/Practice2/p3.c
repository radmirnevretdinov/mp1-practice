#include <stdio.h>
#include <math.h>
#include <stdlib.h>
#include <time.h>
void main()
{
    printf("vv");
	int a[5];
	int i;
	for( i = 0; i < 5; i++)
	   {
		   scanf("%d", &a[i]);
	   }
	for(i = 0; i < 5; i++)
	   {
		   printf("%d", a[i]);
	   }
}